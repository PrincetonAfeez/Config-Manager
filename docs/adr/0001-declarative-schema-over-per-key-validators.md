# ADR 0001: Declarative Schema Over Per-Key Validators

Use `Schema` and `Field` objects to declare the config shape. One declaration
drives defaults, coercion, validation, documentation, init output, masking, and
explain behavior.
